// import {newRealFetcherExecutionContext} from '@codahq/packs-sdk/dist/development';
import {newRealFetcherExecutionContext} from '../../testing/execution';
import {pack} from './simple';

async function foo() {
  const context = newRealFetcherExecutionContext(pack, require.resolve('./simple'));
  const response = await context.fetcher.fetch({method: 'GET', url: 'https://example.com'});
  // console.log(response);
  response!;
}

void foo();
